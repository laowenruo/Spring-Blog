<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mashiro.mapper.ScheduleJobLogMapper">

    <resultMap id="log" type="cn.isbut.entity.ScheduleJobLog">
        <id property="logId" column="log_id"/>
        <result property="jobId" column="job_id"/>
        <result property="beanName" column="bean_name"/>
        <result property="methodName" column="method_name"/>
        <result property="params" column="params"/>
        <result property="status" column="status"/>
        <result property="error" column="error"/>
        <result property="times" column="times"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!--新增定时任务日志-->
    <insert id="saveJobLog" parameterType="cn.isbut.entity.ScheduleJobLog" useGeneratedKeys="true" keyProperty="logId">
        insert into tb_schedule_job_log (job_id, bean_name, method_name, params, status, error, times, create_time)
        values (#{jobId}, #{beanName}, #{methodName}, #{params}, #{status}, #{error}, #{times}, #{createTime})
    </insert>

    <!--删除日志-->
    <delete id="deleteJobLogByLogId">
        delete from tb_schedule_job_log where log_id = #{LogId}
    </delete>

    <!--获取日志列表-->
    <select id="getJobLogListByDate" resultMap="log">
        select log_id, job_id, bean_name, method_name, params, status, error, times, create_time
        from tb_schedule_job_log
        <where>
            <if test="startDate!=null and startDate!='' and endDate!=null and endDate!=''">
                create_time between #{startDate} and #{endDate}
            </if>
        </where>
    </select>
</mapper>
